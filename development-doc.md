# 地图相册与联机拍摄 — 开发交互与重构文档

## 概述

本文档记录了在本次开发迭代中，针对 `memo-home.html` (首页地图)、`camera-prototype.html` (相机页) 以及 `connect-prototype.html` (联机拍摄页) 的核心功能开发、逻辑重构与 UI/UX 优化的完整过程。

---

## 一、高德地图原生接入与坐标系重构

### 1. 真实地图替换
- 废弃了原有的 CSS Transform 模拟地图，全面接入 **高德地图 JS API v2.0**。
- 实现了真实的地图拖拽、双指缩放、平滑过渡等原生交互体验。
- 坐标体系由原先的百分比 (`x/y`) 全面替换为真实的经纬度 (`lng/lat`)。

### 2. 细化程度与缩放级别双向绑定
- 地图缩放级别 (Zoom) 与页面的细化程度 (Level) 深度绑定：
  - `Zoom <= 14` 对应 **Level 1 (低/聚合)**
  - `Zoom == 15` 对应 **Level 2 (中/默认)**
  - `Zoom >= 16` 对应 **Level 3 (高/细分)**
- 用户操作左下角的 `+/-` 细化控件，会自动调用 `amap.zoomIn()` / `amap.zoomOut()`；反之，在地图上滑动滚轮也会自动更新细化程度 UI。

---

## 二、自由标点与前端动态聚合 (Cluster)

### 1. 数据源原子化
- **废弃硬编码**：删除了原先写死的 Level 1 和 Level 2 假数据。
- **唯一数据池**：仅保留 `Level 3` 作为最底层的原子坐标数据池。

### 2. 实时动态聚合算法
- 引入 **单链接聚类 (Single-Linkage Clustering)** 算法。
- 在用户缩小地图 (切换到 Level 1/2) 时，前端基于原子数据实时计算物理距离（经纬度换算）。
- 距离相近的点会自动合并计算出新的质心（Centroid）作为聚合点位置。

### 3. 极简聚合 UI
- 移除了聚合点右上角的数字徽标 (`.cluster-badge`)。
- 当两个或多个点重合时，视觉上仅合并为一个干净的坐标点，避免数字带来的视觉干扰，保持地图界面的清爽。

### 4. 自由标点机制
- 将点击事件绑定到高德原生的 `amap.on('click')`。
- **视觉区分**：自由标记产生的新点统一为 **橙红色 (#FF7043)**，以区分于系统默认的蓝色主坐标。
- **自动吸附**：在 Level 1/2 状态下打点，新点会立即写入 Level 3 数据池，并瞬间参与聚合计算，若距离过近会自动“吸附”融入已有坐标，并弹出 Toast 提示。

---

## 三、拍照自动归类闭环 (跨页面通信)

### 1. 业务流程
- 用户在 `camera-prototype.html` 点击快门拍照。
- 页面生成带有当前时间戳的模拟照片数据，存入 `localStorage` (键值：`pending_new_photos`)，并自动跳转回首页。

### 2. 自动落点与归类
- 首页 `memo-home.html` 加载时自动检测 `localStorage`。
- 获取当前地图的中心点经纬度 `amap.getCenter()`。
- **匹配逻辑**：在 500 米阈值范围内查找最近的已有坐标点。
  - **若存在**：照片自动归入该点，并更新该点的最新时间。
  - **若不存在**：在当前中心点新建一个坐标点，存入照片。
- 归类完成后，弹出 Toast 提示用户归类结果，实现了“拍照即落点”的核心业务闭环。

---

## 四、像素蛋糕 (PixCake) 风格沉浸式联机工作台

### 1. 联机过渡动画
- 在 `connect-prototype.html` 中，点击“开始检测”后，经过 1.5s 的模拟检测，底部会平滑弹出专业的 `.workspace` 联机工作台。

### 2. 专业参数与取景器
- **Live View 取景器**：超大实时画面，附带三分线构图网格。
- **参数控制台**：横向排布的相机核心参数面板 (ISO, F, S, WB, EV)，状态栏实时显示相机型号与电量。

### 3. 实时色彩预设 (Live Color Grading)
- 底部提供类似像素蛋糕的滤镜滑块（原生、日系人像、胶片色彩、黑白等）。
- 点击滤镜，取景器画面通过 CSS `filter` 实时渲染色彩变化，实现“即拍即调”的视觉体验。

### 4. 极速拍摄反馈
- 底部居中的大号快门按钮，点击触发屏幕白光闪烁与设备震动。
- 拍摄后，右下角的“相册缩略图”自动飞入新照片，并继承当前选择的滤镜色彩，右上角红底数字角标实时更新拍摄张数。

---

## 五、灵感模式与 UI 交互统一

### 1. 灵感坐标映射
- 修复了灵感坐标（红色他人点位）在切换真实地图后丢失的问题。
- 编写强制映射函数，将灵感点位散布到当前主坐标（如五四广场）周围，确保在当前可视区域内可见。

### 2. 上传模式 (Upload Mode) 弹窗统一
- **废弃旧版上传弹窗**：删除了之前独立的 `.upload-popup` 组件。
- **复用标准弹窗**：在 `?mode=upload` 模式下，点击坐标点直接复用首页的黑色标准弹窗 (`.marker-popup`)。
- **无缝照片勾选**：
  - 在标准弹窗内为照片 (`.tg-photo`) 增加了点击选中逻辑。
  - 选中时照片叠加黑色半透明遮罩与橙色对勾 (`.tg-photo-check`) 动画。
  - 动态底部操作栏：勾选照片后，弹窗底部自动滑出操作栏，显示已选张数并提供“确认上传”按钮，实现极致的交互统一。

---

## 六、下一步优化建议 (Next Steps)
1. **真实接口对接**：目前的 `localStorage` 跨页面通信可替换为后端的真实 API 调用。
2. **安全密钥配置**：需在 `memo-home.html` 补充高德地图真实的 `securityJsCode` 以消除控制台的 API 鉴权警告。
3. **性能优化**：当 Level 3 原子数据达到上千个时，可考虑引入 Web Worker 来运行聚类算法，避免阻塞主线程 UI 渲染。